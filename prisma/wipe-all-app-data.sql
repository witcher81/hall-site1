-- =============================================================================
-- אזהרה: מוחק את כל המשתמשים ואת כל הנתונים התלויים בהם (אולמות, הודעות,
-- פניות, ביקורות וכו') — זה מיועד ל־DB פיתוח / איפוס מלא בלבד.
-- לא להריץ על פרודקשן בלי גיבוי!
-- =============================================================================
-- הרצה מקומית (עם DATABASE_URL ב־.env):
--   npx prisma db execute --file prisma/wipe-all-app-data.sql --schema prisma/schema.prisma
-- או ב־Neon: Dashboard → SQL Editor → הדבק והרץ.
-- =============================================================================

BEGIN;

TRUNCATE TABLE
  "User"
RESTART IDENTITY CASCADE;

COMMIT;
